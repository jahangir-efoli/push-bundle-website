/**
 * Editorial review disclosure shown on every blog post (E-E-A-T trust signal)
 * and mirrored into the BlogPosting JSON-LD (`reviewedBy`). Kept in one place so
 * the visible copy and the structured data can never drift apart.
 */
export const REVIEW_TEAM_NAME = "PushBundle Technical Support Team";

export const REVIEW_DISCLOSURE =
  "This article was reviewed by the PushBundle Technical Support Team, who regularly helps Shopify merchants test bundle setup, product selection rules, bundle pricing, cart behavior, and checkout-related bundle issues.";
